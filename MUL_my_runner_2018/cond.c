



((all->p.position.x <= all->mb1.position.x) && (all->p.position.x + 70
                                                (all->p.position.y <= all->mb1.position.y && a >= all->mb1.position.y)){



    if((all->p.position.x >= all->mb1.position.x) && (b <= all->mb1.position.x) &&
//       (all->p.position.x >= c) && (b <= c) &&
       (all->p.position.y >= all->mb1.position.y && a <= all->mb1.position.y) ){//&&
//       (all->p.position.y >= d) && a <= d)){  
